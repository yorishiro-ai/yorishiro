/target
*.swp
*.swo
.env
.env.local
.cargo-cache
.worktrees/
/models
# `ee/web/` carries its own .gitignore for `dist/` and `node_modules/`, including the
# `!dist/.gitkeep` exception the embed needs.
